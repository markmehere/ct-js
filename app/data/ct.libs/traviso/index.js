(() => {
    ct.traviso = {
        kill(level, sprite) {
        },

        exit(level, sprite, exitRoom) {
        },

        exitToLevel(level, sprite, exitLevel) {

        },

        start(level) {
        }
    };
})();
